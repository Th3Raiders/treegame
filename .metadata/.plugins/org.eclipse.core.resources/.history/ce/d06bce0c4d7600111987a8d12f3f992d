package binarytree;

import java.util.Comparator;

import node.Node;
import node.StdAVLNode;
import node.StdValuedNode;
import tree.BSTTree;
import tree.StdBSTTree;
import tree.StdTree;
import tree.Tree;

public class Pdt {
	    public static void main(String[] args) {
	    	
	    	{
	    		
	    		BSTTree<StdAVLNode<Integer>, Integer> t =  new StdBSTTree<StdAVLNode<Integer>, Integer>(Comparator.naturalOrder(),StdAVLNode::new);
	    		for(int i = 0; i < 10; i++) {
	    			t.add(0 + (int)(Math.random() * ((50 - 0) + 1)));
	    		}
	    		
	    		t.printTree();
	    	}
	    }
}
