/*
 * Modélise le noeud d'un arbre binaire valué, des valeurs peuvent lui être associé.
 *  * @cons <pre>
 *     $DESC$ Un noeud n'ayant pas de sous arbres gauche ni droit.
 *     $ARGS$ -
 *     $POST$ getLeft() == null
 *     		  getRight == null </pre>
 */


package node;

public interface ValuedNode<N extends ValuedNode<N, T>, T> extends Node<N> {
	

	// REQUETES

	
	
	public T getValue();

	// COMMANDES

	public void setValeur(T v);
	
	
}
